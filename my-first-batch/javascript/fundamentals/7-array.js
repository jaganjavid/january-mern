
const numbers = [5,33,53,56,76,23,4,67,89,2];

const fruits = ["apple", "Kiwi", "Orange"];

let val;

val = numbers;


// Get the Array Length
// val = numbers.length;

// Cheak if is array
// val = Array.isArray(numbers);

// Get a Single value
// val = numbers[0];

// Insert into array

// val = numbers[1] = 58;

// Find the index of value
// val = numbers.indexOf(88); 
// if array match is return the index number eles return -1

// Mutable Array

// Add on the end
// val = numbers.push(1000);

// Add on the front
// numbers.unshift(2000);

// Take of from the end
// numbers.pop();
// numbers.pop();

// Take off from the front
// numbers.shift();
// numbers.shift();

// Splice -- remove & add

// const fruits = ["apple", "Kiwi", "Orange"];

// To Remove
// fruits.splice(1,1);

// To add
// fruits.splice(fruits.length,0,"Lemon");

// Reverser the array

// val = numbers.reverse();



console.log(val);

